{
  "id": "nucleoid:games_played",
  "query": {
    "sql": {
      "query": "SELECT player_id, COUNT(distinct game_id) AS total FROM player_statistics GROUP BY player_id ORDER BY total DESC",
      "player": "player_id",
      "value": "total",
      "value_type": "uint"
    }
  }
}
